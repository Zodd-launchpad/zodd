{
  "name": "zcash-launchpad-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "test": "tsx src/lib/bondingCurve.test.ts",
    "prisma:generate": "prisma generate",
    "postinstall": "prisma generate",
    "start": "prisma generate && prisma db push --accept-data-loss && tsx src/server.ts"
  },
  "dependencies": {
    "@fastify/cors": "^9.0.1",
    "@prisma/client": "^5.20.0",
    "fastify": "^4.28.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "prisma": "^5.20.0",
    "tsx": "^4.23.13",
    "typescript": "^5.9.3"
  }
}
