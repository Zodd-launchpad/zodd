export default function DemoBanner() {
  return (
    <div className="demo-banner">
      <span className="demo-banner-dot" />
      DEMO — this is a work-in-progress preview, not the final version
    </div>
  );
}
